{
  "name": "FCP-Returns-Helper",
  "packageManager": "yarn@4.6.0",
  "type": "module",
  "scripts": {
    "build": "tsup src/index.ts --minify --format esm --out-dir dist",
    "dev": "tsup src/index.ts --watch --minify --format esm --out-dir dist",
    "start": "node dist/index.js"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3"
  }
}
